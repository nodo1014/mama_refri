# 마마냉장고 개발 사전 지식

## 1. 프로젝트 개요

- **앱 이름**: 마마냉장고 (Mama Refrigerator)
- **목적**: 냉장고 식재료 유통기한 관리, 요리 추천, 재료 낭비 방지
- **타겟 사용자**: 엄마(고델 컨셉)와 초등학생 딸(주디 컨셉)
- **앱 컨셉**: 냉장고 관리를 도와주는 '마마'와 함께 식재료 관리하기

## 2. 기술 스택

### 2.1 핵심 기술

- **프레임워크**: React Native (Expo)
- **언어**: TypeScript
- **상태 관리**: React 훅스 (useState, useEffect)
- **로컬 저장소**: AsyncStorage
- **API 통신**: Fetch API

### 2.2 주요 패키지

```json
{
  "dependencies": {
    "@react-native-async-storage/async-storage": "^2.1.2",
    "@react-navigation/native": "^7.1.6",
    "@react-navigation/native-stack": "^7.3.10",
    "expo": "~53.0.5",
    "expo-notifications": "~0.31.1",
    "expo-status-bar": "~2.2.3",
    "react": "19.0.0",
    "react-native": "0.79.2",
    "react-native-safe-area-context": "^5.4.0",
    "react-native-screens": "^4.10.0",
    "react-native-vector-icons": "^10.2.0"
  }
}
```

## 3. 프로젝트 구조

```
mama_refri/
├── App.tsx                # 앱 진입점 및 내비게이션 설정
├── app.json               # Expo 설정
├── index.ts               # Expo 앱 등록
├── package.json           # 의존성 관리
├── assets/                # 이미지 및 리소스 파일
└── src/                   # 소스코드
    ├── components/        # 재사용 컴포넌트
    │   ├── AddFoodButton.tsx
    │   ├── AllFoodList.tsx
    │   ├── FoodItem.tsx
    │   ├── MamaSpeechBubble.tsx
    │   ├── RecipeRecommendation.tsx
    │   └── UrgentFoodList.tsx
    ├── screens/           # 화면 컴포넌트
    │   ├── AddScreen.tsx
    │   ├── HomeScreen.tsx
    │   └── SettingsScreen.tsx
    ├── types/             # 타입 정의
    │   └── index.ts
    └── utils/             # 유틸리티 함수
        ├── deepseekAPI.ts
        ├── notifications.ts
        ├── recipeUtils.ts
        ├── sampleData.ts
        └── storage.ts
```

## 4. 핵심 데이터 구조

### 4.1 Food 인터페이스

```typescript
export interface Food {
  id: string;
  name: string;
  storageLocation: "refrigerator" | "freezer" | "other";
  expirationDate: string;
  createdAt: string;
}
```

### 4.2 RecipeResponse 인터페이스

```typescript
export interface RecipeResponse {
  title: string;
  ingredients: string[];
  steps: string[];
  tips: string;
}
```

## 5. 주요 기능 및 구현 가이드

### 5.1 식재료 관리 (AsyncStorage)

```typescript
// 저장 예시
const saveFoods = async (foods: Food[]): Promise<void> => {
  try {
    const jsonValue = JSON.stringify(foods);
    await AsyncStorage.setItem("@mama_refri:foods", jsonValue);
  } catch (e) {
    console.error("Error saving foods", e);
  }
};

// 로드 예시
const loadFoods = async (): Promise<Food[]> => {
  try {
    const jsonValue = await AsyncStorage.getItem("@mama_refri:foods");
    return jsonValue != null ? JSON.parse(jsonValue) : [];
  } catch (e) {
    console.error("Error loading foods", e);
    return [];
  }
};
```

### 5.2 유통기한 체크 로직

```typescript
// 유통기한 계산 (며칠 남았는지)
const getDaysRemaining = (expirationDate: string): number => {
  const today = new Date();
  today.setHours(0, 0, 0, 0);

  const expDate = new Date(expirationDate);
  expDate.setHours(0, 0, 0, 0);

  const diffTime = expDate.getTime() - today.getTime();
  const diffDays = Math.ceil(diffTime / (1000 * 60 * 60 * 24));
  return diffDays;
};

// 유통기한 임박 식재료 필터링
const getUrgentFoods = (foods: Food[]): Food[] => {
  return foods.filter((food) => {
    const daysRemaining = getDaysRemaining(food.expirationDate);
    return daysRemaining <= 3;
  });
};
```

### 5.3 DeepSeek API 활용 (AI 레시피 추천)

DeepSeek API는 레시피 추천을 위한 AI 서비스입니다. 사용 전 API 키 설정이 필요합니다.

```typescript
// API 호출 예시
const callDeepSeekAPI = async (
  prompt: string,
  apiKey: string
): Promise<RecipeResponse> => {
  try {
    const response = await fetch(
      "https://api.deepseek.com/v1/chat/completions",
      {
        method: "POST",
        headers: {
          "Content-Type": "application/json",
          Authorization: `Bearer ${apiKey}`,
        },
        body: JSON.stringify({
          model: "deepseek-chat",
          messages: [
            {
              role: "system",
              content:
                "당신은 요리 레시피를 잘 아는 요리 전문가입니다. 주어진 식재료로 만들 수 있는 요리 레시피를 JSON 형식으로 추천해주세요.",
            },
            {
              role: "user",
              content: prompt,
            },
          ],
          temperature: 0.7,
          max_tokens: 1000,
        }),
      }
    );

    if (!response.ok) {
      throw new Error(`API 요청 실패: ${response.status}`);
    }

    const data = await response.json();
    const content = data.choices[0].message.content;
    const jsonMatch = content.match(/\{[\s\S]*\}/);

    if (!jsonMatch) {
      throw new Error("API 응답에서 JSON 형식을 찾을 수 없습니다.");
    }

    return JSON.parse(jsonMatch[0]);
  } catch (error) {
    console.error("DeepSeek API 호출 오류:", error);
    throw error;
  }
};
```

### 5.4 푸시 알림 구현

```typescript
// 알림 권한 요청
const requestNotificationPermission = async (): Promise<boolean> => {
  const { status } = await requestPermissions();
  return status === "granted";
};

// 유통기한 알림 예약
const scheduleExpirationNotification = async (food: Food): Promise<void> => {
  if (!notificationsEnabled) return;

  const daysRemaining = getDaysRemaining(food.expirationDate);

  if (daysRemaining <= 3 && daysRemaining > 0) {
    await scheduleNotificationAsync({
      content: {
        title: "유통기한 임박 알림",
        body: `${food.name}의 유통기한이 ${daysRemaining}일 남았습니다!`,
      },
      trigger: {
        hour: 9,
        minute: 0,
        repeats: false,
      },
    });
  }
};
```

## 6. 개발 환경 설정

### 6.1 개발 환경 구성

1. Node.js, npm 설치
2. Expo CLI 설치: `npm install -g expo-cli`
3. Xcode 설치(iOS 개발 시)
4. Android Studio 설치(Android 개발 시)

### 6.2. 프로젝트 시작

```bash
# 프로젝트 클론 또는 새로 생성
npx create-expo-app mama_refri --template

# 의존성 설치
cd mama_refri
npm install

# 개발 서버 실행
npx expo start
```

### 6.3 테스트 및 디버깅

- iOS 시뮬레이터: `i` 키 입력
- Android 에뮬레이터: `a` 키 입력
- 웹 브라우저: `w` 키 입력
- Expo Go 앱으로 QR 코드 스캔(실기기 테스트)

### 6.4 빌드 명령어 (배포 시)

```bash
# iOS 빌드
eas build -p ios

# Android 빌드
eas build -p android
```

## 7. UI/UX 가이드라인

### 7.1 색상 팔레트

- 주요 색상: `#a880ff` (보라색, 앱 테마 색상)
- 보조 색상: `#4f9cf9` (파란색, 버튼 등)
- 상태 색상:
  - 위험(유통기한 당일): `#ff6b6b`
  - 주의(3일 이내): `#ffab5e`
  - 정상: `#4cd964`

### 7.2 컴포넌트 스타일링 예시

```typescript
const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: "#fff",
    padding: 16,
  },
  title: {
    fontSize: 18,
    fontWeight: "bold",
    marginBottom: 8,
  },
  button: {
    backgroundColor: "#a880ff",
    padding: 12,
    borderRadius: 8,
    alignItems: "center",
  },
  buttonText: {
    color: "#fff",
    fontWeight: "600",
  },
});
```

## 8. 성능 최적화 팁

1. **불필요한 렌더링 방지**: React.memo 및 useCallback 활용
2. **큰 목록 렌더링**: FlatList, SectionList 사용하여 최적화
3. **이미지 최적화**: 적절한 크기로 리사이징
4. **네트워크 요청 캐싱**: API 응답 캐싱 전략 구현
5. **메모리 관리**: 큰 데이터 구조는 필요할 때만 메모리에 로드

## 9. 문제 해결 가이드

### 9.1 일반적인 오류

- **Metro 번들러 오류**: `npm start -- --reset-cache`로 캐시 초기화
- **모듈 로드 실패**: 올바른 경로 확인 및 필요한 패키지 설치 확인
- **iOS 빌드 오류**: Xcode/CocoaPods 버전 호환성 확인
- **Android 빌드 오류**: gradle 버전 및 JDK 버전 확인

### 9.2 디버깅 팁

- **React Native Debugger** 활용
- **console.log** 대신 **console.warn**으로 디버그 메시지 표시 (더 눈에 띔)
- Expo 개발 서버에서 **Debug Remote JS** 활성화
